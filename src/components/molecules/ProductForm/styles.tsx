import { StyleSheet } from 'react-native';

const errorStyles = StyleSheet.create({
    errorText: {
        color: 'red',
    },
    errorBorder: {
        borderColor: 'red',
    },
});

export default errorStyles;
